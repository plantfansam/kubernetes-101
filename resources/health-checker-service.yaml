apiVersion: v1
kind: Service
metadata:
  name: health-checker
spec:
  ports:
    - name: default
      port: 80
      targetPort: 5000
      protocol: TCP
  selector:
    app: health-checker
  type: LoadBalancer
